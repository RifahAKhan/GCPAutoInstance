name: postgresql-infrastructure
runtime:
  name: nodejs
  options:
    packagemanager: npm
description: To build an infrastructure where we setup postgreSQL using pulumi using typescript in GCP. 
config:
  pulumi:tags:
    value:
      pulumi:template: typescript
